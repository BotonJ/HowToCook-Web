/**
 * Canonical 4-dimensional flavor vector type.
 * Single source of truth for the flavor data shape across all components.
 */

export interface FlavorVector {
  sweet: number;
  sour: number;
  umami: number;
  spicy: number;
}
